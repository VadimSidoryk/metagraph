---
id: P1004
type: process
title: Scratch Triage — Daily Graph Writer
created: 2026-04-03
modified: 2026-04-03
status: active
assignee: agent
schedule: "daily at 08:00"
tags: [triage, scratch, graph, tasks, knowledge]
---

## Purpose

Read `scratch.md`, classify each unprocessed entry, and propose graph nodes to the user. After user confirmation, create the approved nodes as proper C/T-series entities. Clear processed entries from scratch.

This is the **classification layer** — it receives output from [[P1003]] (email/Slack filter) and manual notes added by the user throughout the day.

---

## Protocol

### Step 1 — Read scratch.md

Read `/metagpraph/scratch.md`. If the file is empty or missing, log a no-op run and exit.

Identify **unprocessed entries**: lines that do not have a `✅` or `🗑️` marker at the end.

### Step 2 — Load triage rules

Read `/metagpraph/triage-rules.md` before classifying anything. This file defines:
- 🟢 HIGH — auto-create without asking
- 🟡 MEDIUM — propose to user (one confirmation)
- 🔵 KNOWLEDGE — propose as C-series knowledge node
- ⚫ DISCARD — delete silently

Apply these rules strictly. When in doubt, prefer MEDIUM over auto-create.

### Step 3 — Classify and act on each entry

For each unprocessed entry, apply triage-rules.md:

**🟢 HIGH confidence entries:** create the node immediately without asking. Show a brief confirmation after the fact: `✅ Created T1001: "Confirm budget with Anton" [high, by Fri]`

**🟡 MEDIUM and 🔵 KNOWLEDGE entries:** present as a batch proposal. Show all at once, not one by one:
```
Proposing to create:

1. [entry] → T-series: "Reply to Marina re onboarding" | medium | null
2. [entry] → C knowledge: "Competitors launched feature X" | low
3. [entry] → T-series: "Review term sheet from Investor X" | high | null

Confirm all? (yes) Or edit individual items.
```

**⚫ DISCARD entries:** mark silently, log count at end.

Also check for each entry: does it reference a person in `context/personal/`? → add to `context_refs`. Does the entry have a meeting name in brackets? → note in task description.

### Step 4 — Create approved nodes

For each approved item:
1. Read `INDEX.md` → get next available ID (T or C series)
2. Update `INDEX.md` immediately (increment counter, register entity)
3. Create the node file in the correct folder using the template from `CLAUDE.md`
4. Fill frontmatter: `creator: process:P1004`, `context_refs` referencing source meeting/person if available
5. Note in `## Graph Changes` which scratch entries were processed

### Step 5 — Mark entries in scratch.md

After processing:
- Approved and created → append `✅ → [[T1001]]` to the line
- Discarded → append `🗑️` to the line

This ensures re-runs don't re-process the same entries.

### Step 6 — Auto-archive stale entries

Any entry older than **14 days** that is still unprocessed → automatically mark as `🗑️ [auto-archived: >14d]` without asking the user.

### Step 7 — Capacity check

If scratch.md has more than **20 unprocessed entries** → prepend a warning at the top of the triage proposal:
```
⚠️ scratch.md contains X unprocessed entries (>20). Recommend clearing or discarding old ones.
```

### Step 8 — Log the run

Append to **Execution History**:

| Date | Entries read | Created (T) | Created (C) | Discarded | Stale auto-archived | Notes |
|---|---|---|---|---|---|---|

### Step 9 — Commit

Commit all created node files + updated scratch.md + updated INDEX.md in a single commit:
```
sync(P1004): triage scratch — N tasks, M knowledge nodes created
```

---

## Scheduled Tasks

| Trigger ID | Schedule      | Description                                 |
|------------|---------------|---------------------------------------------|
| —          | daily at 08:00 | Triage scratch.md, propose graph nodes      |

> Prompt for the scheduled agent:
> *"Run process P1004 from the metagraph at /Users/vadim/metagpraph"*

---

## Execution History

| Date | Entries read | Created (T) | Created (C) | Discarded | Auto-archived | Notes |
|------|-------------|-------------|-------------|-----------|---------------|-------|
| —    | —           | —           | —           | —         | —             | —     |

---

## Improvement Recommendations

<!-- Append after each run: YYYY-MM-DD: <recommendation> -->

## Related

- [[P1003]] — upstream process that writes to scratch.md
